# Managed-By: AI-Prompting-Library
# Cleanup Protection List
# 
# This file lists files that should NEVER be deleted during cleanup.
# Generated by AI-Prompting-Library propagation system.
# 
# How it works:
# 1. Read this file before any cleanup in the repository
# 2. Files listed here (or containing "Managed-By: AI-Prompting-Library") are protected
# 3. After cleanup completes, verify all protected files still exist
# 4. If any protected files are missing, restore them from the propagation source
#
# Format: One file/pattern per line
# Lines starting with # are comments
# Wildcards are supported (e.g., *.md)

## Protected Files

# Propagated instruction files
AGENTS.md
topic-insights.md
git-github-best-practices.md
.cleanup-protect
audit-folder-quality.ps1

# Archive folder (session history)
archive/

# Any file containing the Managed-By marker (automatically protected)
*.md

## Protected Patterns

# Files with the protection marker
**/*Managed-By: AI-Prompting-Library*

## Verification Check

After any cleanup:
1. List all files with "Managed-By: AI-Prompting-Library" marker
2. Compare against this list
3. If any are missing, restore from template before continuing work

## Important

- NEVER delete files listed here
- NEVER delete files containing "Managed-By: AI-Prompting-Library"
- ALWAYS verify cleanup didn't remove protected files
- If protected files are missing, restore them before proceeding
